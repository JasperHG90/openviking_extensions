"""Tests for the retrieval subsystem.

A package rather than a bare directory so module names stay distinct: two
subsystems both want a ``test_config.py``, and without this pytest sees one
basename twice and refuses to collect.
"""
