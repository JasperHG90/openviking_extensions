{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["vitest/globals"]
  },
  "//": "Root-level *.ts by glob, not by name: listing vitest.config.ts by hand left vitest.integration.config.ts unchecked, and CI runs it. .mjs is not listed because without allowJs tsc ignores it, so naming esbuild.mjs here only looked like coverage.",
  "include": ["src/**/*.ts", "tests/**/*.ts", "*.ts"]
}
